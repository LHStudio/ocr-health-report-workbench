# Docker 中 OCR API 容器访问模型服务的地址。
# 此值保留自对方原始 Docker app.py；部署前请在目标机确认该地址可访问。
PADDLEOCR_VL_SERVER_URL=http://172.18.0.2:8118/v1

# OCR API 容器内用于保存临时上传和输出结果的目录。
OCR_SERVICE_BASE_DIR=/app/pdf_to_excel_service

# 如目标机 Docker 使用另一种 GPU/CUDA 环境，请按其基础镜像调整。
PADDLE_PACKAGE=paddlepaddle-gpu==3.2.0
